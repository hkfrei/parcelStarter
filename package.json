{
  "name": "parcelstarter",
  "version": "1.0.0",
  "description": "a starter template for a js application bundled wirth parcel 2 and eslint support.",
  "author": "Karten-Werk GmbH, Hanskaspar Frei.",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^7.7.0",
    "parcel": "^2.0.0-beta.1"
  },
  "scripts": {
    "lint": "eslint src/**/*.js",
    "dev": "parcel serve src/index.html",
    "prebuild": "rm -rf ./dist",
    "build": "parcel build src/index.html"
  },
  "dependencies": {
    "prettier": "^2.0.5"
  }
}
